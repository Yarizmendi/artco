
export const metadata = {
	title: "My first blog post",
	description: "An awesome blog post about important stuff",
	date: new Date('2023-12-24'),
	author: 'donis.dev'
};

#### Learning Articles

#### This error comes when getting data from the mongo database using mongoose schema instead of using swr toJson method. 
- Use `toObject()` to resolve on client side
- use .lean() before .exec() on server side when querying by mongoose schema
Warning: Only plain objects can be passed to Client Components from Server Components. Objects with toJSON methods are not supported. Convert it manually to a simple value before passing it to props.
  [{$__: ..., $isNew: false, _doc: ...}]

##### Can only render plain object error 
- caused by server responses containing data types that are not React Serializable

##### Instance Mode Vs Global Mode p5JS 

##### Can only render plain object error 
- caused by server responses containing data types that are not React Serializable

##### Can not pass functions as props [in Client Components]
- caused by not appening "Action" to server actions 
- Necessary to enable passing server actions to the client as event handlers

##### Other unclassified
- Formdata and Mongo Objectss are examples of server data that may have to be
converted using methods like `formData.get()` and `Array.from()`

##### 
- Error: Server Functions cannot be called during initial render. This would create a fetch waterfall. Try to use a Server Component to pass data to Client Components instead. 

  [cause]: AggregateError [ETIMEDOUT]: 
      at internalConnectMultiple (node:net:1117:18)
      at internalConnectMultiple (node:net:1185:5)
      at Timeout.internalConnectMultipleTimeout (node:net:1711:5)
      at listOnTimeout (node:internal/timers:575:11)
      at process.processTimers (node:internal/timers:514:7) 
    code: 'ETIMEDOUT',
    [errors]: [ [Error], [Error], [Error], [Error] ]

    
```js
const advancing: boolean = true
```

  