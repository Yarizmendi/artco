
import Sketch from "../tests/Test"

export default async function Page({ params }) {
    // const [id, type, title] = params.path.split("-")
    return <Sketch displayName={"test"} images={[]} inputs={[]} />
  }




